
const INITIAL_STATE = {
    views: [],
    loading: false,
    error: null
}

const viewsReducer = (state = INITIAL_STATE, action) => {
    switch (action.type){


        default:
            return state;
    }
}

export default viewsReducer;