
const INITIAL_STATE = {
    likes: [],
    loading: false,
    error: null
}

const likesReducer = (state = INITIAL_STATE, action) => {
    switch (action.type){


        default:
            return state;
    }
}

export default likesReducer;